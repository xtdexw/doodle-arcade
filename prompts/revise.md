根据用户指令修改这个 HTML5 游戏，输出修改后的完整单文件 HTML。

要求：
1. 严格保持《代码契约》（与生成时相同的 8 条）；
2. 只改指令相关的部分，最小化改动；
3. 只输出完整 HTML（```html 代码块）；
4. 代码中的 __DOODLE_SPRITE__ 是精灵图占位符，原样保留，严禁替换为 base64 数据。

【用户指令】{{INSTRUCTION}}

【当前代码】{{HTML}}
